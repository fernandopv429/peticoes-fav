# Dependências de PRODUÇÃO — é isto que entra na imagem do Coolify.
# Ferramenta de desenvolvimento vai em requirements-dev.txt.

fastapi>=0.115
uvicorn>=0.32
pydantic>=2.9
anthropic>=0.69
httpx>=0.28

# Montagem do PDF: o timbrado da 1ª página é diferente do das demais, então a
# peça é renderizada duas vezes e as páginas são intercaladas aqui.
# NÃO é opcional: sem pypdf o `pdf.py` cai no fallback silencioso e TODAS as
# páginas saem com o logo largo da primeira, sem a faixa do rodapé.
pypdf>=5.1
